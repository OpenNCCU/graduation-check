const requireComparator = (require, credit, replaceCredit) => {
  credit.forEach((element) => element);
  replaceCredit.forEach((element) => element);
  return {
    ...require,
  };
};

export default requireComparator;
